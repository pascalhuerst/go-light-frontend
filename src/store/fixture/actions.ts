import Vue from 'vue';
import { ActionTree } from 'vuex';
import { RootState } from '../types';
import { FixtureState } from './types';

export const actions: ActionTree<FixtureState, RootState> = {
};
